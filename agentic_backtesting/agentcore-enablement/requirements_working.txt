# Core packages - install these first
bedrock-agentcore>=1.0.0
strands-agents>=1.0.0
pandas>=1.5.0
backtrader==1.9.78.123
boto3>=1.26.0
python-dotenv>=1.0.0
httpx>=0.24.0
bedrock-agentcore-starter-toolkit>=0.1.0

# Install pyiceberg separately to avoid conflicts
# pip install "pyiceberg[s3fs]>=0.5.0" --no-deps
# Then install its dependencies manually if needed
