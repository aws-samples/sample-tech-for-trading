# AWS Configuration
# Copy this file to .env.local and fill in your values

# ============================================
# FOR LOCAL DEVELOPMENT (npm run dev)
# ============================================
# These are used server-side by Next.js API routes
AWS_REGION=us-east-1
AGENTCORE_ARN=arn:aws:bedrock-agentcore:us-east-1:600627331406:runtime/test

# Note: AWS credentials are NOT needed in .env files
# - Locally: Uses AWS CLI credentials from ~/.aws/credentials
# - Amplify: Uses IAM role attached to the Amplify app
# AWS_ACCESS_KEY_ID=not_needed_locally
# AWS_SECRET_ACCESS_KEY=not_needed_locally

# ============================================
# FOR STATIC DEPLOYMENT (./deploy.sh)
# ============================================
# These are embedded at build time for client-side use
# Must have NEXT_PUBLIC_ prefix to be accessible in browser
NEXT_PUBLIC_AWS_REGION=us-east-1
NEXT_PUBLIC_AGENTCORE_ARN=arn:aws:bedrock-agentcore:us-east-1:600627331406:runtime/test
NEXT_PUBLIC_AWS_ACCESS_KEY_ID=your_access_key_here
NEXT_PUBLIC_AWS_SECRET_ACCESS_KEY=your_secret_key_here

# ============================================
# OPTIONAL SETTINGS
# ============================================
# Use mock data instead of real AgentCore (for testing)
NEXT_PUBLIC_USE_MOCK_DATA=false

# Custom S3 bucket name for deployment (optional)
# S3_BUCKET_NAME=my-custom-bucket-name
